# ประวัติการเปลี่ยนแปลง

รูปแบบเวอร์ชันใช้ `ปี.เดือน.วัน.ลำดับบิลด์`

## 2026.08.11.9.1

- ลดขนาดตัวอักษรค่า eGFR ในฉลาก Auto เป็น `12pt`
- บังคับขนาดเดียวกันทั้งโหมดปกติ Compact และ Dense
- ใช้ Bootstrap Update ที่ไม่แทนที่ Controller เพื่อหลีกเลี่ยง file lock บนเครื่องที่ยังอัปเดต `.9` ไม่สำเร็จ
- คง Updater รุ่นแก้ไขที่ retry file lock สูงสุด 45 วินาที

## 2026.08.11.9

### แก้ระบบอัปเดต

- แก้ `WinError 5` เมื่อ process แม่ของ PyInstaller ยังล็อก `AutoprintController.exe`
- เพิ่มการ retry ทุก 0.5 วินาที สูงสุด 45 วินาที เฉพาะข้อผิดพลาด file lock (`WinError 5/32`)
- Bootstrap Update รอบนี้ไม่รวม `AutoprintController.exe` เพื่อให้ Updater รุ่นเก่าติดตั้งแพ็กเกจได้โดยไม่ชนไฟล์ที่กำลังถูกใช้งาน
- ติดตั้ง `AutoprintUpdater.exe` รุ่นใหม่สำหรับรองรับการแทนที่ไฟล์ runtime ในเวอร์ชันถัดไป
- ยังคงรวมไฟล์ PHP และการปรับฉลาก Auto ทั้งหมดจาก `.8`

### หมายเหตุ

- เครื่องใหม่ใช้ Setup `.9` ซึ่งเป็นชุดเต็ม
- เครื่องเดิมใช้ Update ZIP `.9` แล้วไม่จำเป็นต้องติดตั้ง Setup ทับ หากการอัปเดตสำเร็จ

## 2026.08.11.8

### ปรับรูปแบบฉลาก Auto

- เพิ่มข้อความทั่วไปเป็น `13.5pt` และระยะบรรทัดเป็น `1.05`
- คงวิธีใช้เป็นข้อความเด่นและรองรับการตัดบรรทัดเมื่อข้อความยาว
- ลดชื่อยาอังกฤษเป็น `11pt` ทุกโหมด
- ลดชื่อหน่วยบริการและที่อยู่เป็น `11pt`; Compact/Dense ใช้ `10.5pt`
- ไม่แสดงบรรทัดข้อมูลยาที่ไม่มีค่า เพื่อลดพื้นที่ว่างที่ไม่จำเป็น
- ขยับเกณฑ์ Compact จากประมาณ 11 เป็น 13 บรรทัด และ Dense จาก 14 เป็น 17 บรรทัด
- ป้องกันชื่อยายาวและวิธีใช้แยกออกเป็นฉลากอีกหน้าโดยไม่จำเป็น

### การเผยแพร่

- Setup เป็นชุดเต็มสำหรับเครื่องใหม่
- Update ZIP เป็น Repair Update เฉพาะไฟล์ระบบที่จำเป็น ไม่รวม `vendor` และค่าตั้งค่าเครื่อง

## 2026.08.11.7

### แก้ไข

- แก้คิวสถานะ `ready/processing/printing` เก่าค้างและขวางการอัปเดต
- สถานะทำงานที่ค้างเกิน 10 นาทีถูกย้ายเป็น `error` ใน `autoprintdb` อย่างปลอดภัย
- เพิ่มชื่อขั้นตอนและ path ที่ผิดพลาดใน `updater.log`
- ทำ rollback แบบเก็บสาเหตุเดิมและบันทึกคำเตือนแยกกัน
- ตัด `.vs`, `__pycache__` และไฟล์พัฒนาที่ไม่จำเป็นออกจากแพ็กเกจ
- ทำ Repair Update ขนาดเล็กสำหรับเครื่องที่ Updater รุ่นเก่าแจ้ง `WinError 3`

### ความปลอดภัยของข้อมูล

- ไม่เปลี่ยนฐาน `jhcisdb` ระหว่างตรวจความพร้อมอัปเดต
- ไม่แตะรายการคิว `pending` และ `waiting`
- ไม่รวม config, รหัสผ่าน, Log, Output หรือข้อมูลผู้ป่วยใน Release

## 2026.08.11.6

- เพิ่มช่องว่างก่อน “สรรพคุณ” และ “วิธีใช้” ของฉลากยา Auto
- ฉลากที่มีข้อความยาวแบบ Compact/Dense ใช้ระยะที่สั้นกว่าเพื่อป้องกันข้อความล้นและหน้าว่าง
- ไม่เปลี่ยนฟอร์มฉลาก Manual

## 2026.08.11.5

- เพิ่มหน้าต่างแจ้งอัปเดตบนหน้าเว็บหลัก
- เลือก “อัปเดตตอนนี้” หรือ “ไว้ภายหลัง” ได้
- Tray ตรวจและดาวน์โหลดอัปเดตก่อนเริ่ม Worker ในการเปิดโปรแกรมครั้งแรก
- หากกดอัปเดตขณะทำงาน ระบบหยุดรับงานใหม่และรอเฉพาะงานที่กำลัง render/print
- ป้องกันการกดติดตั้งซ้ำและการดาวน์โหลด ZIP หลายเวอร์ชันค้างพร้อมกัน
- เพิ่ม `update_api.php` พร้อม CSRF โดยหน้าเว็บส่งคำสั่ง ส่วน Tray เป็นผู้ติดตั้งจริง
- หากอินเทอร์เน็ตขัดข้อง ระบบพิมพ์ยังสามารถเริ่มทำงานได้

## 2026.08.11.4

- เพิ่มการตั้งค่าชื่อหน่วยบริการ ที่อยู่ และเบอร์โทรศัพท์จากหน้า Debug
- ใช้ข้อมูลหน่วยบริการเดียวกันกับฉลาก Manual และ Auto
- เก็บค่าในตาราง `facility_settings` ของฐาน `autoprintdb`
- รักษาค่าตั้งค่าเดิมเมื่ออัปเดต

## 2026.08.11.3

- เริ่มระบบอัปเดตอัตโนมัติผ่าน GitHub Releases และ `latest.json`
- ตรวจสอบเวอร์ชันตามรอบที่กำหนด โดยค่าเริ่มต้นทุก 6 ชั่วโมง
- ตรวจขนาดและ SHA-256 ของ Update ZIP ก่อนติดตั้ง
- สำรองไฟล์เดิมและรองรับ rollback เมื่อ schema หรือการติดตั้งล้มเหลว
- ป้องกันการเขียนทับ config และโฟลเดอร์ข้อมูลเฉพาะเครื่อง

## 2026.08.11.2

- ปรับชุดติดตั้งสำหรับเครื่องที่มี Laragon อยู่แล้ว
- เพิ่มการติดตั้งโครงสร้างฐาน `autoprintdb` ผ่าน `setup_db.php`
- เพิ่มช่อง HCC/Hospital Code ในตัวติดตั้ง
- เพิ่มการเคลียร์คิวตกค้างพร้อมรหัสยืนยันที่เก็บใน `autoprintdb`
- ปรับการจัดหน้าเมื่อชื่อยาและวิธีใช้ยาว เพื่อลดฉลากเปล่าคั่น

## รุ่นก่อนหน้า

- วางระบบพิมพ์ฉลากยา Manual และ Auto รุ่นเริ่มต้น
- เชื่อมข้อมูลจาก JHCIS และสร้างคิวใน `autoprintdb`
