# คู่มือใช้งานไฟล์ Release 2026.08.11.9.5

เอกสารนี้อธิบายการใช้งานไฟล์ที่เผยแพร่สำหรับระบบ **New Label JHCIS Autoprint** รุ่น `2026.08.11.9.5` เป็นภาษาไทย

> เอกสารและไฟล์ข้อความใน repository นี้บันทึกเป็น **UTF-8** เพื่อให้ภาษาไทยแสดงผลถูกต้องบน GitHub, Notepad และโปรแกรมแก้ไขข้อความสมัยใหม่

## เลือกไฟล์ให้ถูก

| ไฟล์ | ใช้เมื่อ | วิธีใช้โดยย่อ |
| --- | --- | --- |
| `NewLabelJHCIS_Setup_2026.08.11.9.5.exe` | เครื่องใหม่ หรือเครื่องเดิมที่อัปเดตอัตโนมัติไม่สำเร็จ | เปิดตัวติดตั้งและทำตามหน้าจอ |
| `NewLabelJHCIS_Update_2026.08.11.9.5.zip` | เครื่องที่ติดตั้งระบบเดิมและมี Laragon/ระบบทำงานอยู่แล้ว | ให้ Tray อัปเดตอัตโนมัติเป็นหลัก; แตก ZIP เองเฉพาะกรณีแก้ไขฉุกเฉิน |
| `SHA256SUMS.txt` | ต้องการตรวจความถูกต้องของไฟล์ก่อนเปิดใช้งาน | เปรียบเทียบค่า SHA-256 ของไฟล์ที่ดาวน์โหลด |
| `latest.json` | ระบบ Tray อ่านอัตโนมัติ | ผู้ใช้ทั่วไปไม่ต้องดาวน์โหลดหรือแก้ไข |

## 1. ติดตั้งบนเครื่องใหม่ด้วย Setup EXE

ใช้ไฟล์ `NewLabelJHCIS_Setup_2026.08.11.9.5.exe` สำหรับเครื่องใหม่ที่มี Laragon แล้ว

1. เปิด Laragon และให้บริการ MySQL ทำงานก่อนเริ่มติดตั้ง
2. ดาวน์โหลด Setup EXE จากหน้า **Releases** แล้วตรวจ SHA-256 ตามหัวข้อ “ตรวจสอบไฟล์” ด้านล่าง
3. ดับเบิลคลิกไฟล์ Setup EXE
4. เลือกโฟลเดอร์ติดตั้ง โดยปกติใช้ `C:\laragon\www\new_labelJHCIS`
5. กรอกข้อมูลเชื่อมต่อ JHCIS ให้ถูกต้อง เช่น Host/IP, Port, Database Name, User, Password และ HCC/Hospital Code
6. ระบุ PHP ของ Laragon และชื่อเครื่องพิมพ์ตามหน้าจอที่ตัวติดตั้งถาม
7. เมื่อติดตั้งเสร็จ ให้เปิด Laragon แล้วเปิด Tray Auto Print
8. เปิดหน้า `http://localhost:8081/new_labelJHCIS/debug.php` เพื่อตรวจการเชื่อมต่อ และกำหนดรหัสยืนยันสำหรับการเคลียร์คิว
9. ทดสอบพิมพ์ฉลาก 1 รายการก่อนเริ่มใช้งานจริง

ตัวติดตั้งจะสร้างหรือปรับตารางของ `autoprintdb` ให้เข้ากับรุ่นใหม่ โดยรักษาข้อมูลเดิมไว้ และไม่ปรับโครงสร้างฐาน `jhcisdb`

## 2. อัปเดตเครื่องเดิมด้วย Tray อัตโนมัติ (แนะนำ)

เครื่องที่ติดตั้ง Tray Auto Print แล้วควรใช้วิธีนี้

1. ตรวจว่า Tray Auto Print กำลังเปิดอยู่ใน System Tray ของ Windows
2. Tray จะตรวจรุ่นใหม่เมื่อเริ่มโปรแกรม และตรวจซ้ำตามรอบที่ตั้งไว้ (ค่าปกติทุก 6 ชั่วโมง)
3. เมื่อพบรุ่น `2026.08.11.9.5` ระบบจะดาวน์โหลด ZIP จาก GitHub, ตรวจ SHA-256 และแสดงสถานะบนหน้าเว็บ
4. เลือก **อัปเดตตอนนี้** เพื่อให้ Tray หยุดงานพิมพ์ที่จำเป็น, ติดตั้งไฟล์, ปรับ schema `autoprintdb` และเริ่มระบบใหม่
5. รอจนหน้าเว็บแจ้งว่าระบบพร้อมใช้งาน แล้วจึงทำงานต่อ

ระหว่างอัปเดต ระบบรักษาไฟล์ตั้งค่าเฉพาะเครื่อง เช่น `config.php`, `Autoprint/autoprint_config.php`, `settings.json`, Log และ Output ไว้ ไม่เขียนทับ

## 3. ใช้ Update ZIP ด้วยตนเอง (เฉพาะกรณีจำเป็น)

ใช้ได้กับเครื่องที่เคยติดตั้งระบบแล้วเท่านั้น **ไม่ใช้แทน Setup สำหรับเครื่องใหม่** เพราะ ZIP เป็นแพ็กเกจอัปเดต ไม่รวมไฟล์ dependency ทุกชุด

ก่อนดำเนินการ ให้แน่ใจว่าไม่มีงานพิมพ์กำลังทำงานอยู่

1. ปิด Tray Auto Print จากไอคอนมุมขวาล่างของ Windows
2. สำรองโฟลเดอร์ `C:\laragon\www\new_labelJHCIS` หรืออย่างน้อยสำรองไฟล์ต่อไปนี้ไว้ก่อน
   - `config.php`
   - `Autoprint/autoprint_config.php`
   - `Autoprint/settings.json`
   - `Autoprint/GUI Start รันเพื่อส่งข้อมูลระบบพิมพ์ Auto/settings.json`
3. แตกไฟล์ `NewLabelJHCIS_Update_2026.08.11.9.5.zip` ไปที่โฟลเดอร์ `C:\laragon\www\new_labelJHCIS`
4. เลือก **แทนที่ไฟล์เดิม** เฉพาะไฟล์จาก ZIP แต่ห้ามลบไฟล์ตั้งค่าที่สำรองไว้
5. เปิด Laragon และเปิด Tray Auto Print ใหม่
6. Tray จะเรียก `Autoprint/setup_db.php` เพื่อเพิ่มโครงสร้างรุ่นใหม่ใน `autoprintdb`
7. เปิดหน้า Debug ตรวจการเชื่อมต่อ และเปิดหน้า Cleanup เพื่อตั้งเวลา “เคลียร์คิวอัตโนมัติหลังพิมพ์” ตามต้องการ

หากไม่แน่ใจว่าเครื่องเดิมเป็นรุ่นใด หรือ ZIP แตกไฟล์แล้วระบบไม่เริ่ม ให้ใช้ Setup EXE ติดตั้งทับในโฟลเดอร์เดิมแทน โดยไม่ถอนโปรแกรมและไม่ลบฐานข้อมูลเดิมก่อน

## 4. ตรวจสอบ SHA-256 ของไฟล์

เปิด PowerShell ในโฟลเดอร์ที่ดาวน์โหลดไฟล์ แล้วใช้คำสั่งต่อไปนี้

```powershell
Get-FileHash .\NewLabelJHCIS_Setup_2026.08.11.9.5.exe -Algorithm SHA256
Get-FileHash .\NewLabelJHCIS_Update_2026.08.11.9.5.zip -Algorithm SHA256
```

ค่าที่ถูกต้องสำหรับรุ่นนี้คือ

```text
B716A1F4D5DAF9F137A3A3A978028FB0BBC164DD2F1F4703500897592E310BDC  NewLabelJHCIS_Setup_2026.08.11.9.5.exe
4CA52B9A794D613CF72577B20C4C26FC5C215CB0B3150E92B9AF9AEAF97B1D1E  NewLabelJHCIS_Update_2026.08.11.9.5.zip
```

หากค่าไม่ตรง ให้ลบไฟล์นั้นแล้วดาวน์โหลดใหม่จากหน้า Releases ห้ามนำไปติดตั้งหรือแตกไฟล์

## 5. จุดที่เพิ่มในรุ่นนี้

- หลังพิมพ์ฉลากสำเร็จ ระบบจะพยายามเปลี่ยน Visit นั้นจาก `Z1/Y1` เป็น `03` (รับบริการเสร็จสิ้นแล้ว)
- หาก JHCIS ถูกบันทึกทับกลับเป็น `Z1/Y1` ภายหลัง ระบบตรวจซ้ำตามเวลาที่กำหนด โดยจำกัดเฉพาะ Visit ที่มีหลักฐานว่าพิมพ์สำเร็จแล้ว
- ตั้งเวลาเปิด/ปิดและกำหนดหลายช่วงเวลาได้จากหน้า `cleanup.php`
- ปุ่ม “เคลียร์คิวตกค้าง Z1/Y1” แบบแมนนวลยังคงอยู่ และยังมีผลกับทุก Visit ที่เป็น Z1/Y1 เพื่อใช้ในกรณีฉุกเฉิน

## 6. หากอัปเดตไม่สำเร็จ

ตรวจไฟล์ต่อไปนี้ แล้วส่งไฟล์หรือข้อความ error ให้ผู้ดูแลระบบ

```text
C:\laragon\www\new_labelJHCIS\Autoprint\update\logs\updater.log
C:\laragon\www\new_labelJHCIS\Autoprint\update\last-update.json
```

หาก Tray เปิดไม่ได้หรือไฟล์ถูก Windows ปิดกั้น ให้ใช้ Setup EXE จากหน้า Release แทนการคัดลอกไฟล์ EXE เอง
